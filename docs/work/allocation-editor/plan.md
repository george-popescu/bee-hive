## Wave 1
- [ ] T1: Adaugă un endpoint tranzacțional care reconciliază toate alocările pentru o persoană și o lună, validând scope-ul, proiectele active, multiplii de 0,25h și egalitatea dintre totalul lunar și distribuțiile săptămânale — done when: `php artisan test --compact tests/Feature/AllocationPlanUpdateTest.php` demonstrează că un request poate crea, actualiza și șterge rânduri atomic, că orice eroare face rollback complet, că utilizatorii fără permisiune sau fără scope primesc 403 și că fiecare schimbare produce audit `allocation.upserted`, `allocation.updated` sau `allocation.deleted`.
  - files: `routes/web.php`, `app/Http/Controllers/AllocationController.php`, `app/Http/Requests/ReplacePersonMonthAllocationsRequest.php`, `app/Services/Capacity/AllocationPlanService.php`, `tests/Feature/AllocationPlanUpdateTest.php`
## Wave 2 (depends on Wave 1)
- [ ] T2: Extinde contractul Team Planning cu draftul complet și istoricul minimal al contextului persoană/lună, inclusiv auditul rândurilor șterse — done when: `php artisan test --compact tests/Feature/TeamLeadPlanPageTest.php` confirmă că payload-ul include toate alocările, distribuțiile săptămânale, autorul și timestamp-ul, că istoricul include create/update/delete pentru persoane din scope și exclude înregistrările altor echipe.
  - files: `app/Services/TeamLead/TeamLeadPlanData.php`, `tests/Feature/TeamLeadPlanPageTest.php`
## Wave 3 (depends on Wave 2 and completion of `docs/work/team-planning-monthly/plan.md`)
- [ ] T3: Înlocuiește dialogul individual cu editorul inline din `06-editor-alocari.html`, legat de celula Monthly selectată și de endpoint-ul tranzacțional — done when: schimbarea persoanei/lunii, adăugarea, mutarea și eliminarea proiectelor, editarea săptămânilor, recalcularea impactului, avertizarea de supra-alocare, Save și Discard funcționează pe date reale; utilizatorul read-only nu vede controale de mutație; `npm run build` și `composer run ci:check` trec; iar verificarea în browser la 1440×1200, 768×1024 și 390×844 trece în light/dark fără erori de consolă.
  - files: `resources/js/components/team-planning/allocation-editor.tsx`, `resources/js/components/team-planning/monthly-capacity-view.tsx`, `resources/js/pages/team-lead/index.tsx`, `resources/js/lib/i18n.ts`
