## Wave 1
- [x] T1: Build the generic Deliverables annex payload from synchronized task scopes, execution estimates, time entries, weekly plans, owners and dates while preserving nullable contract fields and classifying the real MiM DEV reference project as Deliverables — done when: `php artisan test --compact tests/Feature/PmDeliverablesBoardTest.php tests/Feature/AdminIndexAndPeopleTest.php` first fails for the missing contract and then proves multiple dynamic annex scopes, an explicit unresolved scope, separate consumption and delivery progress, current-week planned vs worked, next-week agreed tasks, nullable contract fields, timeline rows, no hardcoded client branch, and idempotent template classification for both `MiM` and `MiM DEV` without touching similarly named projects.
  - files: `app/Services/PmBoard/PmBoardData.php`, `database/migrations/2026_07_15_132915_configure_reference_project_templates.php`, `tests/Feature/PmDeliverablesBoardTest.php`, `tests/Feature/AdminIndexAndPeopleTest.php`
## Wave 2 (depends on Wave 1)
- [x] T2: Route Deliverables projects to a faithful React rendition of `02-project-board-anexe.html` with project, Week/Month and annex controls, three KPIs, annex health or planned-vs-delivered, agreed work and contract timeline — done when: all visible copy and values come from the tested payload; missing contract/owner/estimate/date states remain explicit; changing project and period uses the generated Inertia route; the annex filter updates all dependent sections locally; `php artisan test --compact tests/Feature/PmBoardPageTest.php tests/Feature/PmDeliverablesBoardTest.php`, `npm run build`, and `composer run ci:check` pass; and Browser screenshots at 1440×1200, 768×1024 and 390×844 in light/dark match template 02 without relevant console errors or uncontrolled overflow.
  - files: `resources/js/components/pm-board/annex-board-view.tsx`, `resources/js/components/pm-board/project-selector-view.tsx`, `resources/js/pages/pm-board/index.tsx`, `resources/js/lib/i18n.ts`, `tests/Feature/PmBoardPageTest.php`, `tests/Feature/PmDeliverablesBoardTest.php`
