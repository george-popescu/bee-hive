## Wave 1
- [x] T1: Canonicalize the PM project-selection data contract so the default route resolves one highest-activity visible project, nullable contract types stay explicitly unconfigured, and the three mapped reference projects receive their approved templates through an idempotent migration — done when: feature tests prove the default single-project selection, explicit `tm`/`deliverables`/`null` props, and the Administration payload preserves `null`; `php artisan test --compact tests/Feature/PmBoardPageTest.php tests/Feature/AdminIndexAndPeopleTest.php` passes.
  - files: `app/Http/Controllers/PmBoardController.php`, `app/Services/PmBoard/PmBoardData.php`, `app/Services/Admin/AdminData.php`, `database/migrations/2026_07_15_132915_configure_reference_project_templates.php`, `lang/en/messages.php`, `lang/ro/messages.php`, `resources/js/pages/admin/index.tsx`, `resources/js/lib/i18n.ts`, `tests/Feature/PmBoardPageTest.php`, `tests/Feature/AdminIndexAndPeopleTest.php`
## Wave 2 (depends on Wave 1)
- [x] T2: Replace the generic PM Boards entry with the dynamic React rendition of `01-selector-proiect.html`, using a single Project selector, Week/Month control, template identity, three KPI cards, estimate-consumption rows, ClickUp timeline, and Next discussion table while preserving Inertia navigation and explicit missing-data states — done when: the selected project and period change through the generated PM Board route, the visible labels and metrics are driven by tested server props, `php artisan test --compact tests/Feature/PmBoardPageTest.php` passes, `npm run lint:check`, `npm run types:check`, and `npm run build` pass, and Chrome screenshots at 1440×1200, 768×1024, and 390×844 in both light and dark match the hierarchy and responsive behavior of template 01 with no console errors.
  - files: `app/Services/PmBoard/PmBoardData.php`, `resources/js/pages/pm-board/index.tsx`, `resources/js/components/pm-board/project-selector-view.tsx`, `resources/js/lib/i18n.ts`, `tests/Feature/PmBoardPageTest.php`
